import "multi_module_worker.cblc";

int accumulator;

void add_once() {
    accumulator = accumulator + 1;
}

void main() {
    accumulator = 0;
    show_banner();
    add_once();
    display(accumulator);
}
