file in "input.txt";
file out "output.txt";
char line[256];

function void process_file() {
    open(in, "r");
    open(out, "w");
    while (read(in, line)) {
        write(out, line);
    }
    close(in);
    close(out);
}

function void main() {
    process_file();
}
