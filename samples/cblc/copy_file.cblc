file in "input.txt";
file out "output.txt";
char line[256];

void process_file() {
    open(in, "r");
    open(out, "w");
    while (read(in, line)) {
        write(out, line);
    }
    close(in);
    close(out);
}

void main() {
    process_file();
}
