function MAIN() {
    if (!(FLAG == true)) {
        while (!(COUNT > LIMIT)) {
            LIMIT = COUNT;
        }
    } else {
        INDEX = 0;
        while (!(INDEX >= LIMIT)) {
            RESULT = INDEX;
            INDEX++;
        }
    }
    return ;
}

function NEXT() {
    FLAG = true;
    return ;
}
