function void MAIN() {
    if (!(FLAG == true)) {
        while (!(COUNT > LIMIT)) {
            LIMIT = COUNT;
        }
    } else {
        INDEX = 0;
        while (!(INDEX >= LIMIT)) {
            RESULT = INDEX;
            INDEX++;
        }
    }
    return ;
}

function void NEXT() {
    FLAG = true;
    return ;
}
